Fix header layout - explicit justify-content: flex-start on the container, with the right-side nav block pushed right via its own margin-left auto (DevTools-confirmed root cause: container had no justify-content override, so Tabler default was spacing children apart)
