Notification list: rebuilt header + rows as a single shared CSS Grid (was independent Bootstrap rows, which could never truly align column widths) - titles now line up exactly with their fields below regardless of content width
