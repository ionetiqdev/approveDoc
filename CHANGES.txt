Distribution UI: notification row reordered (Days, When, Channel, Send to, Person, Delete), Days field 20% wider, tighter gap after Before/After, delete icon pinned to line end. Start Date now input-group matching Due Date width, whole row bottom-aligned.
