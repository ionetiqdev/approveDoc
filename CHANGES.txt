Audiences and Notifications lists now have a fixed 290px height (sized for ~5 rows) with internal vertical scroll - modal no longer resizes as rows are added. Notification grid header is sticky so it stays visible while rows scroll beneath it.
