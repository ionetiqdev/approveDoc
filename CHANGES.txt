Distribution modal restructured: single-column layout (narrower, taller) instead of two side-by-side columns to fix scaling issues on smaller windows. Tabs (Audiences/Notifications) moved below the fields. Document + Distribution type on same line. Require password moved beside Instructions.
