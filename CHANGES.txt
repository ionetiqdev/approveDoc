Notifications: reconciled with the direct-sending work from the other session. send-due-notifications now calls send-notification-direct (Slack/Resend/ClickSend directly) instead of the Make webhook - Make scenario untouched, kept in background. Committed source for send-notification-direct and a migration for ad_notification_template, both of which previously existed only on Supabase with no git-tracked source.
