Distribution UI polish: notification list has one shared header (not per-row), red trash delete icon for consistency; Start Date label moved left of input; 15px padding above Require Password; Distribution Type defaults to One Off
